{
    "watch": ["src"],
    "ext": "ts",
    "exec": "npx kill-port 3000 && ts-node -r tsconfig-paths/register src/main.ts",
    "ignore": ["node_modules", "dist"],
    "restartable": "rs",
    "env": {
      "NODE_ENV": "development",
      "PORT": "3000"
    }
  }